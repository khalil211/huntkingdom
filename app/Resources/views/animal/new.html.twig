{% extends 'baseAdmin.html.twig' %}
{% block title %}Ajouter animal{% endblock %}
{% block titre %} Ajouter animal {% endblock %}
{% block content %}

    {{ form_start(form) }}
        {{ form_widget(form) }}
        <input type="submit" value="Create" />
    {{ form_end(form) }}

    <ul>
        <li>
            <a href="{{ path('animal_index') }}">Back to the list</a>
        </li>
    </ul>
{% endblock %}
